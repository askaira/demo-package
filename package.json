{
  "name": "demo-package",
  "version": "0.1.1",
  "scripts": {
    "test": "echo \"no tests\" && exit 0",
    "build": "echo build succeeded",
    "release": "standard-version"
  },
  "devDependencies": {
    "standard-version": "^9.5.0"
  }
}
